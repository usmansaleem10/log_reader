# frozen_string_literal: true

RSpec.describe LogReader::Reader do
  it "receives only one input" do
    expect(false).to eq(true)
  end
  it "receives reveives file path with .log extension" do
    expect(false).to eq(true)
  end
  it "received file has data in it" do
    expect(false).to eq(true)
  end
  it "receives only one input" do
    expect(false).to eq(true)
  end
  it "returns error if file has an error" do
    expect(false).to eq(true)
  end
  it "returns error if file has wrong extension" do
    expect(false).to eq(true)
  end
  it "adds a new path in data if not there already" do
    expect(false).to eq(true)
  end
  it "updates path unt coin data if its there already" do
    expect(false).to eq(true)
  end
  it "adds a new ip in data if not there already" do
    expect(false).to eq(true)
  end
  it "updates the ip count in  data if its there already" do
    expect(false).to eq(true)
  end
  it "has file as attr_reader" do
    expect(false).to eq(true)
  end
  it "has error as attr_reader" do
    expect(false).to eq(true)
  end
  it "has json_data as attr_reader" do
    expect(false).to eq(true)
  end

  it "has total count equal to the sum of idivisual ip count" do
    expect(false).to eq(true)
  end
end
